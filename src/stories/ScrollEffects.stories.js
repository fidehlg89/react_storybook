import ScrollEffects from "../components/ScrollEfffects/ScrollEffects";

export default {
  title: "Components/ScrollEffects",
  component: ScrollEffects,
};

const Template = (args) => <ScrollEffects {...args} />

export const Basic = Template.bind({});

Basic.args = {

};
